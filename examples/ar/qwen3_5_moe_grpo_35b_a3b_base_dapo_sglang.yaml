# @package _global_
# AR GRPO Qwen3.5-MoE text-only on DAPO-Math — train_ar (ARTrainer),
# SGLang rollout, full-weight TensorWeightSync, colocate.
#
# MoE variant: VeOmni EP8 shards experts on train side; SGLang TP8/EP8 serves
# rollout weights through the TP/EP-aware TensorWeightSync path from #226.
# Install the VeOmni extra: uv pip install -e ".[sglang,train,infer,veomni]"
#
# QWEN3_5_MOE_PATH accepts a Hub ID or local checkpoint; meta-init resolves and
# validates Hub shards in the local cache before loading.
#
# Smoke (1 GPU, 1 rollout, tiny model):
#   QWEN3_5_MOE_PATH=/path/to/local/qwen3.5-moe-tiny \
#     python -m unirl.train_ar --config-name=ar/qwen3_5_moe_grpo_35b_a3b_base_dapo_sglang \
#     num_devices=1 num_rollouts=1 backend.fsdp_cfg.ep_size=1 \
#     rollout.config.tp_size=1 rollout.config.ep_size=1
#
# Real run (8 GPU):
#   DATA_PATH=data/dapo_math/train.jsonl EVAL_DATA_PATH=data/dapo_math/aime_eval.jsonl \
#   QWEN3_5_MOE_PATH=/path/to/Qwen3.5-35B-A3B \
#   python -m unirl.train_ar --config-name=ar/qwen3_5_moe_grpo_35b_a3b_base_dapo_sglang

num_devices: 8
batch_size: 64
num_rollouts: 800
weight_sync_interval: 1
eval_interval: 10
enable_fsdp_offload: true

adv_normalization_scope: group
normalize_adv_by_std: true

logging:
  report_to_wandb: true
  project_name: unirl-grpo
  run_name: grpo_qwen3_5_moe_text_dapo_sglang
  entity: ${oc.env:WANDB_ENTITY,null}
  tags: [grpo, qwen3_5, moe, text, dapo, sglang]

bundle:
  _target_: unirl.models.qwen3_5.bundle.Qwen3_5Bundle.from_config
  config:
    _target_: unirl.models.qwen3_5.config.Qwen3_5PipelineConfig
    pretrained_model_ckpt_path: ${oc.env:QWEN3_5_MOE_PATH}
    # VeOmni materializes bf16 compute parameters; FSDP keeps fp32 masters below.
    model_precision: bf16
    use_gradient_checkpointing: true
    attn_implementation: null
    trust_remote_code: false
    meta_init_transformer: true

pipeline:
  _target_: unirl.models.qwen3_5.pipeline.Qwen3_5Pipeline.from_bundle
  enable_thinking: true
  autocast_precision: bf16
  logprob_precision: fp32

backend:
  _target_: unirl.train.backend.veomni.backend.VeOmniBackend
  block_class_names: ["Qwen3_5MoeDecoderLayer"]
  trainable_attr: transformer
  fsdp_cfg:
    _target_: unirl.train.configs.FSDPConfig
    param_dtype: bf16
    master_dtype: fp32
    cpu_offload: false
    mixed_precision: true
    fsdp_mode: full
    reshard_after_forward: true
    activation_checkpointing: true
    use_torch_compile: false
    forward_prefetch: false
    defer_grad_sync: false
    sp_size: 1
    ep_size: 8
  optimizer_cfg:
    _target_: unirl.train.backend.base.OptimizerConfig
    learning_rate: 1.0e-6
    adam_beta1: 0.9
    adam_beta2: 0.999
    adam_epsilon: 1.0e-8
    weight_decay: 0.01
  scheduler_cfg:
    _target_: unirl.train.backend.base.LrSchedulerConfig
    type: constant
    warmup_steps: 0
    total_steps: 1000

rollout:
  _target_: unirl.rollout.engine.sglang.engine.SGLangRolloutEngine
  config:
    _target_: unirl.rollout.engine.sglang.config.SGLangEngineConfig
    backend: http
    pretrained_model_ckpt_path: ${oc.env:QWEN3_5_MOE_PATH}
    tp_size: 8
    ep_size: 8
    concurrency: 64
    samples_pre_expanded: true
    response_forbidden_tokens: ["<|image_pad|>", "<|video_pad|>"]
    enable_memory_saver: true
    enable_weights_cpu_backup: true
    skip_server_warmup: false
    chat_template_kwargs:
      enable_thinking: true
    engine_kwargs:
      mem_fraction_static: 0.15
      attention_backend: triton
      disable_cuda_graph: false
      cuda_graph_max_bs: 64
      enable_lora: false

reward:
  _target_: unirl.reward.service.RewardService
  truncated_reward: keep
  backend:
    _target_: unirl.reward.local.mathverify.MathVerifyRewardScorer
    base_device: cpu
    config:
      _target_: unirl.reward.local.mathverify.MathVerifySpec

algorithm:
  _target_: unirl.algorithms.grpo.GRPO
  stage_attr: ar
  clip_range: 0.2
  clip_range_high: null
  clip_schedule: constant
  loss_agg_mode: seq-mean-token-mean
  horizon: ${sampling.max_new_tokens}
  sampling_temperature: ${sampling.temperature}
  conditions_cls:
    _target_: hydra.utils.get_class
    path: unirl.models.qwen3_5.conditions.Qwen3_5ARConditions

sync:
  _target_: unirl.distributed.weight_sync.full.tensor.TensorWeightSync
  lora_merged: false
  bucket_size_mb: 512
  wire_dtype: bf16
  flush_cache: true
  name_remap: {"model.visual.*": null, "model.language_model.*": "model.*"}

stack:
  _target_: unirl.train.stack.TrainStack
  micro_batch_size: 1
  max_grad_norm: 1.0
  num_updates_per_batch: 4
  micro_planner:
    _target_: unirl.train.stack.TokenBudgetPlanner
    token_budget: 10240

data_source:
  _target_: unirl.data.data_source.MultimodalRLDataSource
  args:
    run:
      data_path: ${oc.env:DATA_PATH}
      eval_data_path: ${oc.env:EVAL_DATA_PATH,${oc.env:DATA_PATH}}
      seed: 42
    algorithm:
      prompts_per_rollout: 64

sampling:
  _target_: unirl.types.sampling.ARSamplingParams
  samples_per_prompt: 8
  temperature: 1.0
  top_p: 1.0
  top_k: 0
  max_new_tokens: 8192
