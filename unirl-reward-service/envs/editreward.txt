# EditReward scorer — Qwen2.5-VL / MiMo-VL 7B multi-head reward model
# for instruction-guided image editing evaluation.
#
# NOTE: On images where Ray runtime_env pip isolation is broken (no pip
# in virtualenv), install these deps directly via the launcher script
# (see launch_flux2klein_editreward_2node.sh). The envs file is still
# required by the service config validator.
#
# Install EditReward package from GitHub:
#   editreward @ git+https://github.com/TIGER-AI-Lab/EditReward.git
# Or add to PYTHONPATH from a local checkout.

pyarrow<19
datasets
pillow
fastapi
uvicorn
pydantic
openai
megfile
sentencepiece
deepspeed
fire
omegaconf
matplotlib
peft
trl==0.8.6
tensorboard
scipy
transformers==4.57.0
accelerate
requests
packaging
pandas
opencv-python
einops
timm
huggingface_hub
qwen-vl-utils
av
regex
safetensors
tqdm
PyYAML
